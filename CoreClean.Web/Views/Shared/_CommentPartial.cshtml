@using CoreClean.Web.Utilities; 
@model CoreClean.Domain.Models.Comment

@*<div class="container mt-5">
    <div class="row d-flex justify-content-center">
        <div class="col-md-8">

            <div class="card p-3">
                <div class="d-flex justify-content-between align-items-center">
                    <div class="user d-flex flex-row align-items-center"> <span><small class="font-weight-bold text-primary">@Model.User.UserName</small> <small class="font-weight-bold">@Model.Text</small></span> </div> <small>@Model.DateCreated</small>
                </div>
                <div class="action d-flex justify-content-between mt-2 align-items-center">
                    <div class="reply px-4"> <small>Remove</small> <span class="dots"></span> <small>Reply</small> <span class="dots"></span> <small>Translate</small> </div>
                    <div class="icons align-items-center"> <i class="fa fa-star text-warning"></i> <i class="fa fa-check-circle-o check-icon"></i> </div>
                </div>
            </div>
            
        </div>
    </div>
</div>*@

            <div class="d-flex flex-row px-3 py-1">
                <div class="w-100">
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="d-flex flex-row align-items-center"> <span class="mr-2">@Model.User.UserName</span> </div> <small>@Model.DateCreated.TimeAgo()</small>
                    </div>
                    <p class="text-justify comment-text mb-0">@Model.Text</p>
                </div>
            </div>