@using Microsoft.AspNetCore.Identity
@using CoreClean.Web.Helpers
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            @*<div class="dropdown">

        <a class="nav-link dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Hello @UserManager.GetUserAsync(User).Result.FirstName !</a>
        <div class="dropdown-menu" aria-labelledby="drodownMenuButton">
        <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Manage/Index">Manage Profile</a>
        <a class="dropdown-item" asp-controller="Photo" asp-action="UserIndex">My Photos</a>
        <a class="dropdown-item" asp-controller="Photo" asp-action="Create">Add Photo</a>
        </div>
        </div>*@
            <div class="dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                    Hello @UserManager.GetUserAsync(User).Result.FirstName !
                </a>

                <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <li><a class="dropdown-item" asp-controller="Profile" asp-action="ViewProfile">Manage Profile</a></li>
                    <li><a class="dropdown-item" asp-controller="Photo" asp-action="UserIndex">My Photos</a></li>
                    <li><a class="dropdown-item" asp-controller="Photo" asp-action="Create">Add Photo</a></li>
                </ul>
            </div>
        </li>
        @*<li class="nav-item">
    <a class="nav-link text-dark dropdown-toggle" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserAsync(User).Result.FirstName !</a>
    </li>*@
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link " asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link " asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>
